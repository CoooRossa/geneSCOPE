% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal_leesL.R
\name{.computeL_visium_internal}
\alias{.computeL_visium_internal}
\title{Compute Lee's L for Visium (internal implementation)}
\usage{
.computeL_visium_internal(
  scope_obj,
  grid_name = NULL,
  norm_layer = "Xz",
  use_idelta = TRUE,
  S_target = 8000L,
  min_detect = 0.1,
  winsor_high = 0.95,
  ncores = NULL,
  backend = "cpp",
  verbose = getOption("geneSCOPE.verbose", TRUE),
  ...
)
}
\arguments{
\item{scope_obj}{A `scope_object` containing Visium data.}

\item{grid_name}{Grid layer name to operate on.}

\item{norm_layer}{Normalised expression layer name.}

\item{use_idelta}{Use Idelta prescreening when TRUE.}

\item{S_target}{Target number of genes after prescreening.}

\item{min_detect}{Minimum detection rate when filtering genes.}

\item{winsor_high}{Winsorization quantile for expression scaling.}

\item{ncores}{Number of threads to use (defaults to safe thread count).}

\item{backend}{Lee's L backend policy. Uses the same C++ -> R executable
contract as `computeL()`; `python` errors explicitly because no equivalent
Python Lee's L backend is shipped.}

\item{verbose}{Emit progress messages when TRUE.}

\item{...}{Additional arguments forwarded to `.compute_l()`, such as
`mem_limit_GB`, `use_bigmemory`, `chunk_size`, `perms`, or
`lee_stats_layer_name`.}
}
\value{
The modified `scope_object`.
}
\description{
Performs Visium-specific prescreening and setup (spatial weights, optional Idelta
prescreening, and gene filtering) before dispatching to `computeL()`.
This is the internal implementation used by the computeL_visium wrapper.
}
\seealso{
`computeL()`, `computeWeights()`, `computeIDelta()`
}
\keyword{internal}
