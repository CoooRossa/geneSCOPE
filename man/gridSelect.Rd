% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gridselect_external.R
\name{gridSelect}
\alias{gridSelect}
\title{Recommend an optimal grid size using the external Idelta grid selector}
\usage{
gridSelect(
  xenium_dir = NULL,
  transcripts_path = NULL,
  roi_csv = NULL,
  bbox_csv = NULL,
  bbox_pad_um = 0,
  out_dir,
  tool_bin = NULL,
  docker_image = NULL,
  apptainer_sif = NULL,
  auto_find = TRUE,
  threads = NULL,
  knee_search_min_um = 5,
  descending_delta_um = 2,
  max_width_um = NULL,
  smooth_method = NULL,
  grid_um_eval = NULL,
  epsilon = 1,
  q_bins = 5,
  ratio_high = 2,
  loss_min = 0.2,
  ...
)
}
\arguments{
\item{xenium_dir}{Xenium outs directory containing `transcripts.csv.gz` (or `transcripts.csv` / `transcripts.parquet`).}

\item{transcripts_path}{Optional explicit transcripts path (overrides `xenium_dir`).}

\item{roi_csv}{ROI vertices CSV/TSV (comments allowed; header may be `X,Y`). If NULL, a bbox ROI is auto-generated.}

\item{bbox_csv}{Optional coordinate CSV/TSV used to auto-generate a bbox ROI when `roi_csv` is NULL (e.g., Xenium `cells.csv.gz`).}

\item{bbox_pad_um}{Optional padding (umm) applied to the auto-generated bbox ROI (default 0).}

\item{out_dir}{Output directory root; creates `staging/`, `tool/`, `plots/`, `logs/`.}

\item{tool_bin}{Optional path to `idelta-gridselect` binary (highest priority).}

\item{docker_image}{Optional docker image to run (next priority).}

\item{apptainer_sif}{Optional Apptainer/Singularity SIF path to run (next priority).}

\item{auto_find}{Logical; if TRUE and no tool is provided, search common local build locations.}

\item{threads}{Number of threads passed to the external tool (`--threads`).}

\item{knee_search_min_um}{Minimum grid width considered when selecting the knee.}

\item{descending_delta_um}{Minimum descending-grid delta used by the external selector.}

\item{max_width_um}{Optional maximum grid width for selector search.}

\item{smooth_method}{Optional smoothing method passed through to the external selector.}

\item{grid_um_eval}{Optional grid size for immediate QC evaluation after selection.}

\item{epsilon}{Near-zero threshold for I-delta QC summaries.}

\item{q_bins}{Number of quantile tiers used by QC summaries.}

\item{ratio_high}{Knee scale mismatch threshold used by QC summaries.}

\item{loss_min}{Loss flag threshold used by QC summaries.}

\item{...}{Extra options: `strict`, `best_grid_style`, `search_roots`, `skip_tool`, `skip_plots`.}
}
\value{
A list with the recommended grid size and output paths.
}
\description{
One-step wrapper that can (i) run the external `idelta-gridselect recommend-grid`
tool on Xenium inputs and (ii) generate best_grid-style plots + summary.
}
\details{
If `xenium_dir`/`transcripts_path`/`roi_csv` are not provided, this function
instead consumes existing outputs under `file.path(out_dir, "tool")` and
regenerates plots/summary under `file.path(out_dir, "plots")`.
}
