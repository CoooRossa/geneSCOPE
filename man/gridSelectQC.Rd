% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gridselect_external.R
\name{gridSelectQC}
\alias{gridSelectQC}
\title{Compute grid-at-choice Idelta QC (dispersion, tiers, unsuitable flags) from external tool outputs}
\usage{
gridSelectQC(
  out_dir = NULL,
  xenium_dir = NULL,
  transcripts_path = NULL,
  roi_csv = NULL,
  bbox_csv = NULL,
  bbox_pad_um = 0,
  threads = 32,
  knee_search_min_um = 5,
  descending_delta_um = 2,
  max_width_um = NULL,
  skip_molecules = FALSE,
  skip_tool = FALSE,
  repo_pin = NULL,
  tool_out_dir = NULL,
  plots_out_dir = NULL,
  grid_um_eval = NULL,
  epsilon = 1,
  q_bins = 5,
  ratio_high = 2,
  loss_min = 0.2,
  best_grid_style = TRUE,
  strict = TRUE,
  log_out_path = NULL,
  flag = NULL
)
}
\arguments{
\item{out_dir}{Optional wrapper output root that contains `tool/` and `plots/`.}

\item{xenium_dir}{Optional Xenium outs directory; when supplied the external tool is run first.}

\item{transcripts_path}{Optional explicit transcripts path; when supplied the external tool is run first.}

\item{roi_csv}{Optional ROI vertices CSV/TSV passed to the external tool.}

\item{bbox_csv}{Optional coordinate CSV/TSV used for bbox ROI generation.}

\item{bbox_pad_um}{Optional padding applied to generated bbox ROI coordinates.}

\item{threads}{Number of threads passed to the external tool.}

\item{knee_search_min_um}{Minimum grid width considered when selecting the knee.}

\item{descending_delta_um}{Minimum descending-grid delta used by the external selector.}

\item{max_width_um}{Optional maximum grid width for selector search.}

\item{skip_molecules}{Skip molecule preprocessing when external tool outputs already exist.}

\item{skip_tool}{Skip external tool execution and consume existing outputs.}

\item{repo_pin}{Optional source repository pin recorded in generated QC metadata.}

\item{tool_out_dir}{Directory containing external tool outputs (`knee_summary.tsv`, `final_curves.tsv.gz`).}

\item{plots_out_dir}{Output directory for QC tables and plots.}

\item{grid_um_eval}{Grid size (umm) to evaluate; defaults to `recommended_grid_um` from `knee_summary.tsv`.}

\item{epsilon}{Near-zero threshold for `idelta_at_grid` (default 1).}

\item{q_bins}{Number of quantile tiers (default 5).}

\item{ratio_high}{Knee scale mismatch threshold (default 2.0), comparing `knee_um` to `grid_um_eval`.}

\item{loss_min}{Loss flag threshold (default 0.2).}

\item{best_grid_style}{When TRUE, generate best_grid-style QC plots (requires ggplot2).}

\item{strict}{When TRUE, emit warnings on missing fields or plotting dependencies.}

\item{log_out_path}{Optional text log path to write the QC summary block.}

\item{flag}{Optional free-text flag written to the QC summary output.}
}
\value{
A list with `qc_summary`, `gene_bins`, file paths, and plot paths.
}
\description{
This is a post-recommendation diagnostic step: it consumes `knee_summary.tsv`
and `final_curves.tsv.gz` under `tool_out_dir` (or `<out_dir>/tool`) and writes
QC tables + plots under `plots_out_dir` (or `<out_dir>/plots`).
}
