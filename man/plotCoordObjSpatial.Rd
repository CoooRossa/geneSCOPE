% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotSegmentation.r
\name{plotCoordObjSpatial}
\alias{plotCoordObjSpatial}
\title{Plot Spatial Segmentation from coordObj with Optional Group Coloring}
\usage{
plotCoordObjSpatial(
  coordObj,
  colors = NULL,
  grid_gap = 100,
  bar_len = 400,
  bar_offset = 0.01,
  arrow_pt = 4,
  title = NULL,
  alpha = 0.5
)
}
\arguments{
\item{coordObj}{A \code{coordObj} object that already contains
\code{coordObj@coord$segmentation} with columns \code{x}, \code{y}, and \code{cell}.}

\item{colors}{Named character vector. Colors for each group; if \code{NULL}, all cells are grey.
If \code{colors} is provided, it should have names corresponding to the
\code{group} column in \code{coordObj@coord$segmentation}. If no \code{group} column exists,
all cells are rendered in grey.}

\item{grid_gap}{Numeric. Grid line spacing (default \code{100}).}

\item{bar_len}{Numeric. Length of the scale bar (default \code{400}, in coordinate units).}

\item{bar_offset}{Numeric. Vertical offset of the scale bar relative to the y‑range
(default \code{0.01}).}

\item{arrow_pt}{Numeric. Arrow‑head size of the scale bar (pt; default \code{4}).}

\item{title}{Character. Plot title; default is "Spatial Segmentation" when \code{NULL}.}
}
\value{
\if{html}{\out{<div class="sourceCode">}}\preformatted{      A `ggplot2` object containing only the plot.
}\if{html}{\out{</div>}}
}
\description{
Draw tissue spatial contours based on the segmentation data stored in a \code{coordObj}.
You can optionally pass a \code{data.frame} (with columns \strong{cell.id} and \strong{group}) to
colour each cell polygon; when omitted, all polygons are rendered in grey.
}
