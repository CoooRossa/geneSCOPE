% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/4.PlotDensity.r
\name{plotDensityMirror}
\alias{plotDensityMirror}
\title{Mirror Plot of Two Gene Densities Across Grids}
\usage{
plotDensityMirror(
  coord_obj,
  layer_name,
  gene1,
  gene2,
  sort_by = c("sum", "diff", "gene1", "gene2"),
  rescale = TRUE
)
}
\arguments{
\item{coord_obj}{A \code{CoordObj} containing a \code{@grid} slot that
includes \code{densityDF}.}

\item{layer_name}{Character. The grid sub-layer to query.}

\item{gene1, gene2}{Character. Names of the two genes to compare.}

\item{sort_by}{Ordering criterion: \code{"sum"}, \code{"diff"},
\code{"gene1"}, or \code{"gene2"}.}

\item{rescale}{Logical. Rescale densities to 0-1 before plotting.}
}
\value{
Invisibly returns the generated \code{ggplot} object.
}
\description{
Creates a mirrored area plot in which the density values of \code{gene1}
are shown above the baseline and those of \code{gene2} are negated and
shown below, producing an intuitive comparison across grids.  The grids
can be ordered by the sum, difference, or individual gene density, and
densities may be min-max rescaled to 0-1 for direct visual comparison.
}
\examples{
\dontrun{
p <- plotDensityMirror(P5.coord, "25um", "GAPDH", "MALAT1",
    sort_by = "diff"
)
plot(p)
}
}
