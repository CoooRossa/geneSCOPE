% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/4.PlotDensity.r
\name{plotGridBoundary}
\alias{plotGridBoundary}
\title{plotGridBoundary}
\usage{
plotGridBoundary(
  scope_obj,
  grid_name,
  colour = "black",
  linewidth = 0.2,
  panel_bg = "#C0C0C0",
  base_size = 10
)
}
\arguments{
\item{scope_obj}{A coord-style S4 object that contains a \verb{@grid} slot,
e.g. produced by the STUtility pipeline.}

\item{grid_name}{Character. The name of the grid layer to plot, e.g.
"grid_grid_length31". Must exist inside \code{scope_obj@grid}.}

\item{colour}{Border colour for grid rectangles. Default "black".}

\item{linewidth}{Border line width for rectangles. Default 0.2.}

\item{panel_bg}{Background colour of the panel. Default "#C0C0C0".}

\item{base_size}{Base font size for \code{theme_minimal()}. Default 10.}
}
\value{
A \code{ggplot} object.
}
\description{
Draw grid cell boundaries for a given grid layer in a coord object and
guarantee that the output image keeps the exact spatial aspect ratio
of the original coordinates (no stretching or squeezing), following the
same padding logic used in \code{plotDensity()}.
}
\examples{
p <- plotGridBoundary(P5.coord, "grid_grid_length31")
plot(p)
}
