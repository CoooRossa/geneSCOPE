% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotIDelta.r
\name{plotIDeltaByCluster}
\alias{plotIDeltaByCluster}
\title{Plot Iδ by Cluster}
\usage{
plotIDeltaByCluster(
  coordObj,
  grid_name = NULL,
  cluster_col,
  suffix = c("raw", "nor"),
  top_n = NULL,
  cluster_palette = c("#E41A1C", "#377EB8", "#4DAF4A", "#FF7F00", "#FFFF33", "#A65628",
    "#984EA3", "#66C2A5", "#FC8D62", "#8DA0CB", "#E78AC3", "#A6D854", "#FFD92F",
    "#E5C494"),
  point_size = 3,
  line_size = 0.5,
  label_size = 3,
  subCluster = NULL
)
}
\arguments{
\item{coordObj}{A \code{coordObj} containing either
\code{coordObj@grid[[grid_name]]$iDeltaStats$delta_raw} or \code{delta_nor},
or a column named \verb{<grid_name>_iDelta_raw} / \verb{<grid_name>_iDelta_nor} in
\code{coordObj@meta.data}. Additionally, \code{coordObj@meta.data} must have a column
indicating each gene’s cluster labels (\code{cluster_col}).}

\item{grid_name}{Character, name of the grid sublayer to use (e.g., \code{"grid_lenGrid50"}).
If NULL and \code{coordObj@grid} has exactly one sublayer, that sublayer is used;
otherwise, this parameter is required.}

\item{cluster_col}{Character, column name in \code{coordObj@meta.data} that indicates gene cluster labels.}

\item{suffix}{Character, either \code{"raw"} or \code{"nor"} (default \code{"raw"}). Determines which Iδ values
to use (\code{delta_raw} or \code{delta_nor}).}

\item{top_n}{Integer (optional). If provided, only the top \code{top_n} genes by Iδ value in each
cluster are displayed (default NULL, no filtering).}

\item{cluster_palette}{Named character vector (optional), mapping cluster names to colors. If not provided,
colors are generated automatically and extended if needed.}

\item{point_size}{Numeric, size of the points (default 3).}

\item{line_size}{Numeric, thickness of the connecting lines (default 0.5).}

\item{label_size}{Numeric, font size for gene labels (default 3).}
}
\value{
A \code{ggplot2} object with one horizontal row of panels—each panel corresponding to one cluster—where
panel widths scale proportionally to the number of genes in that cluster. Points and connecting lines
show each gene’s Iδ value, and each gene is labeled by name. Gene labels at the far left/right are not clipped.
}
\description{
Plot Iδ by Cluster
}
