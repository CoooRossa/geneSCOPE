% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz_classes.r
\name{processSegmentation}
\alias{processSegmentation}
\title{Process Segmentation Boundaries}
\usage{
processSegmentation(
  seg_file,
  tag = "cell",
  flip_y = FALSE,
  y_max = NULL,
  keep_cells = NULL,
  ncores = 1
)
}
\arguments{
\item{seg_file}{Path to the segmentation boundaries file (Parquet format).}

\item{tag}{Character tag to label the segmentation type (e.g., "cell" or "nucleus").}

\item{flip_y}{Logical. Whether to flip the y-coordinates (default \code{FALSE}). If \code{TRUE}, argument \code{y_max} must be provided.}

\item{y_max}{Numeric. The maximum Y value for flipping coordinates (required if \code{flip_y = TRUE}).}

\item{keep_cells}{Optional vector of cell IDs. If provided, only segmentation data for these cells will be retained.}

\item{ncores}{Integer. Number of cores for parallel polygon processing (default 1).}
}
\value{
A list with two components: \code{points} (a data.table of segmentation points with columns \code{cell}, \code{x}, \code{y}, \code{label_id}) and \code{polygons} (an \code{sfc} object of polygon geometries).
}
\description{
Reads a segmentation boundaries file (e.g., cell or nucleus boundaries) and processes it into coordinate data and polygon geometries.
}
\details{
This function requires the \pkg{arrow} package to read Parquet files. After reading the segmentation data, it flips the coordinates if needed, filters by cell IDs if provided, and then constructs polygon geometries for each segmented object.

Author: FG Team. Created: 2025-07-05.
}
