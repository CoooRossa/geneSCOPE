% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_backward_compat_guard.R
\name{runBackwardCompatGuard}
\alias{runBackwardCompatGuard}
\title{Run the strict backward-compatibility guard.}
\usage{
runBackwardCompatGuard(
  package_root = NULL,
  baseline_path = NULL,
  output_dir = tempdir(),
  verbose = TRUE
)
}
\arguments{
\item{package_root}{Path to the geneSCOPE source tree. Defaults to the nearest
parent directory containing `DESCRIPTION`.}

\item{baseline_path}{Path to the pre-change baseline JSON. Defaults to
`inst/extdata/backward_compat/legacy_contract_baseline.json` under
`package_root`.}

\item{output_dir}{Directory where comparison artefacts should be written.}

\item{verbose}{Emit progress messages when TRUE.}
}
\value{
A named list describing the current contract, the comparison result,
  and written artefact paths.
}
\description{
Captures the current legacy contract of the geneSCOPE source tree and compares
it with a pinned pre-change baseline. The guard is intentionally explicit
about the legacy baseline boundary: runtime digests for legacy constructors
and `computeWeights()` are captured under a controlled R fallback harness,
while historical Lee's L runtime digests remain omitted from this guard and
are covered by the dedicated native/reference backend tests.
}
