% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_legacy_exact_guard.R
\name{runLegacyExactGuard}
\alias{runLegacyExactGuard}
\title{Run the strict legacy exactness guard.}
\usage{
runLegacyExactGuard(
  package_root = NULL,
  baseline_path = NULL,
  output_dir = tempdir(),
  verbose = TRUE
)
}
\arguments{
\item{package_root}{Path to the geneSCOPE source tree. Defaults to the nearest
parent directory containing `DESCRIPTION`.}

\item{baseline_path}{Path to the strict baseline JSON. Defaults to
`inst/extdata/backward_compat/legacy_exact_contract_baseline.json` under
`package_root`.}

\item{output_dir}{Directory where comparison artefacts should be written.}

\item{verbose}{Emit progress messages when TRUE.}
}
\value{
A named list describing the current strict contract, the comparison
  result, and written artefact paths.
}
\description{
Captures a stricter legacy contract than `runBackwardCompatGuard()` by
digesting actual grid payloads, sparse weights, normalised matrices, and a
wider set of frozen legacy files. This guard exists to prove exact numerical
legacy preservation during additive redesign work.
}
