% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_phenocycler_contract_guard.R
\name{runPhenoCyclerContractGuard}
\alias{runPhenoCyclerContractGuard}
\title{Run the PhenoCycler v1 additive-contract guard.}
\usage{
runPhenoCyclerContractGuard(
  package_root = NULL,
  baseline_path = NULL,
  output_dir = tempdir(),
  verbose = TRUE
)
}
\arguments{
\item{package_root}{Path to the geneSCOPE source tree. Defaults to the
nearest parent directory containing `DESCRIPTION`.}

\item{baseline_path}{Path to the PhenoCycler v1 baseline JSON. Defaults to
`inst/extdata/backward_compat/phenocycler_contract_baseline.json` under
`package_root`.}

\item{output_dir}{Directory where comparison artefacts should be written.}

\item{verbose}{Emit progress messages when TRUE.}
}
\value{
A named list describing the current contract, the comparison result,
  and written artefact paths.
}
\description{
Captures the current contract of the previously introduced additive
PhenoCycler v1 module and compares it with a pinned baseline. This guard is
separate from `runBackwardCompatGuard()` so the diffuse-redesign work can
prove it did not change current PhenoCycler v1 defaults.
}
