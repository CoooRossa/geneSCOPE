% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_styleB_diagnostics.R
\name{styleB_check_realW}
\alias{styleB_check_realW}
\title{Check style-B variance assumptions on a real weight matrix.}
\usage{
styleB_check_realW(
  W,
  x = NULL,
  y = x,
  X = NULL,
  max_pairs = 25L,
  n_perm = 199L,
  seed = NULL,
  center_scale = TRUE,
  warn_ratio_threshold = 2
)
}
\arguments{
\item{W}{Spatial weight matrix.}

\item{x}{Numeric observed vector for the first feature. Optional when `X`
is supplied.}

\item{y}{Numeric observed vector for the second feature. Defaults to `x`.}

\item{X}{Optional feature matrix with rows matching `nrow(W)` and columns as
genes/features. If supplied, a bounded set of feature pairs is checked.}

\item{max_pairs}{Maximum number of feature pairs to check when `X` is
supplied.}

\item{n_perm}{Number of permutations used for the null variance estimate.}

\item{seed}{Optional random seed.}

\item{center_scale}{Center and scale `x` and `y` before computing Lee's L.}

\item{warn_ratio_threshold}{Warn when a permutation/style-B variance ratio
is outside `[1 / warn_ratio_threshold, warn_ratio_threshold]`.}
}
\value{
A list with analytic variance, permutation variance, ratio, observed
  L, permutation summary, and assumption flags.
}
\description{
Computes the analytic style-B variance term for a supplied spatial weight
matrix and compares it with a permutation-estimated null variance for one
observed vector pair, or for a bounded set of feature pairs from a real
expression matrix supplied through `X`. This is a diagnostic helper for assessing whether the
large-sample, symmetric-binary, zero-diagonal style-B assumptions are
reasonable for a specific graph. It is not used by the main `computeL()`
inference path.
}
\examples{
\dontrun{
out <- styleB_check_realW(scope_obj@grid$grid30$W, x, y, n_perm = 499)
out$variance_ratio_perm_over_styleB
matrix_out <- styleB_check_realW(scope_obj@grid$grid30$W, X = expr, n_perm = 199)
matrix_out$summary
}
}
